---
title: Tags
subtitle: '- sorted alphabetically'
permalink: /tags/
---
{% set tags = collections.tagList | sort %}

<small class="color:secondary taglist-subtitle">{{ tags.length }} tags {{ subtitle }}</small>
<ol class="tags-list capitalise" role="list">
    {% for tag in tags %}
    {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
        <li><a href="{{ tagUrl | url }}#main" class="post-tag-item">{{ tag }}</a></li>
    {% endfor %}
</ol>